This package contains the core logic of Slate. Feel free to poke around to learn more!

Note: A number of source files contain extracted interface types. This is done currently to support custom value typing through the package-local interfaces in `packages/slate/src/interfaces/`.
