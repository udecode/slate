---
"@slate/yjs": patch
---

Add the first-party Yjs collaboration package scaffold with operation-level text, node, remove, split, merge, move, set, replace-children, replace-fragment, insert-fragment, delete-fragment, wrap, unwrap, and lift collaboration support, plus selection relative-position conversion, split undo repair, and virtual merge/wrapper refs for concurrent remote edits. Pin the audited Yjs UndoManager stack contract and keep broad replace-fragment fallback traceable as an identity-risk path.
