---
"slate-react": minor
---

- Add `props.slots.contentRoot(slot, options)` for rendering editable content roots from element renderers.
- Keep keyboard navigation and history focus on the active mounted copy when one content root is rendered in multiple places.
